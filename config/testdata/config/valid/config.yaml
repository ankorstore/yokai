app:
  name: default-app
  description: default-description
  env: dev
  version: default-version
  debug: false
config:
  values:
    string_value: default
    int_value: 0
  placeholder: foo-${BAR}-baz
  substitution: foo
